import { Meta } from '@storybook/addon-docs';

<Meta title="Introduction" />

# Welcome to the Storybook

This Storybook is divided into two sections, <strong>Form</strong> and <strong>UI</strong> components. 
All the components were styled with `styled-components` npm package.
